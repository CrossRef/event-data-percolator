(ns event-data-percolator.util.storage
  (:require [event-data-common.artifact :as artifact]))

(defn get-domain-list
  "Get a cached copy of the domain list, along with the version."
  []
  ; TODO
)
